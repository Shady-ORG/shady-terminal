# The build chooses a JDK with jlink/jpackage for Compose Desktop packaging.
# Homebrew's JDK 21 works for local DMG builds even though Compose warns about that vendor.
compose.desktop.packaging.checkJdkVendor=false
